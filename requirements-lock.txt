# Exact environment the results of record were produced under.
#
# pyproject.toml carries loose `>=` floors, which is right for someone who
# just wants to run the model. It is not enough to reproduce a published
# number: `pip install -e .[dev]` resolves to whatever is current on the day,
# and a NumPy release that changes a sampler or a reduction moves the answer
# with nothing to show for it.
#
# The reproducibility workflow installs from THIS file. Regenerating the
# results of record after a bump here is a deliberate act with its own commit.
#
# Verified on Python 3.14.3, Linux x86-64, 2026-07-25.
#
# 
# COMPLETE TRANSITIVE CLOSURE, not just the direct dependencies. The workflow installs with
# `--no-deps`, which is deliberate -- it stops pip resolving anything fresh at run time -- but it
# also means a package missing from this file is simply absent. The first version listed the five
# direct pins only, and CI failed on `ModuleNotFoundError: No module named 'dateutil'` because
# pandas imports it at module load. Regenerated by resolving the five into a clean venv and taking
# the full freeze, then verified by installing THIS file with `--no-deps` into another clean venv
# and importing all four runtime packages.
#

iniconfig==2.3.0
numpy==2.4.2
packaging==26.3
pandas==3.0.1
pluggy==1.6.0
pyarrow==25.0.1
Pygments==2.21.0
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
six==1.17.0
