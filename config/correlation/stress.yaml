# Overlay: stress correlation structure — stronger speed-encroachment coupling,
# speed-dispersion coupling, deployment-task coupling, and a direct
# response-magnitude coupling (drivers who respond also slow more).
meta:
  tag: corr_stress
correlation:
  pairs:
    - [mu_v_kmh, r_E_per_veh_km, 0.5]
    - [mu_v_kmh, sigma_v_kmh, 0.3]
    - [v_walk_m_s, t_handle_s, -0.3]
    - [p_R, deltaV_kmh, 0.4]
